#include "Particles.h"

/*
pip::ParticleEffect::ParticleEffect(Vector2D position, PARTICLE_FUNC emitFunc, PARTICLE_FUNC updateFunc, PARTICLE_RENDER_FUNC renderFunc)
{
	this->position = position;
	this->emitFunc = emitFunc;
	this->updateFunc = updateFunc;
	this->renderFunc = renderFunc;
}

void pip::ParticleEffect::emit(unsigned int count)
{
	for(int i = 0; i < count; i++)
		this->emitFunc(new pip::ParticleEffect::Particle());
}

void pip::ParticleEffect::update(double dt)
{
	this->totalElapsed += dt;
	for(auto particle : this->particles)
		this->updateFunc(particle);
}

void pip::ParticleEffect::render()
{
	this->renderFunc(this->particles);
}
*/
